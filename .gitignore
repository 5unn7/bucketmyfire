node_modules/
dist/
*.local
.DS_Store

# Local leaderboard secrets — never commit (.env.example is the checked-in template)
.env
.env.*
!.env.example

# Local tooling / scratch — not part of the game, kept off the repo
.mcp.json
.info.txt
.claude/settings.local.json
scripts/.verify-*.mjs

.vite/
*.log
.playwright-mcp/
dogfood-chase.png
.gstack/

# Raw model sources — kept locally to regenerate optimized assets, not shipped
model-src/
public/animals/ultimate_3d_animal_pack_free_download/

# Dev verification artifacts (screenshots + standalone preview pages)
*.jpeg
*.png
!public/**/*.png
!public/**/*.jpeg
heli-preview.html
icons-preview.html

# Standalone QA/screenshot harnesses + their output — drive on-disk Playwright Chromium to
# regenerate shots locally (e.g. .qa-shot.cjs responsive QA, .og-shot.cjs the OG card). Local-only.
.*-shot.cjs
.qa/
